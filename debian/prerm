#!/bin/bash
set -e

# Update icon cache before removal (ignore errors)
if [ -x /usr/bin/gtk-update-icon-cache ]; then
    /usr/bin/gtk-update-icon-cache -q /usr/share/icons/hicolor 2>/dev/null || true
fi
