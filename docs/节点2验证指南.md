# 节点 2 验证指南

**验证日期**: 2026-04-07
**验证状态**: 待验证

---

## 验证环境

- **开发环境**: macOS (当前)
- **目标环境**: UOS V20 专业版 1070 (amd64)
- **Qt 版本**: Qt 5

---

## 快速验证

### 1. 编译测试程序

```bash
cd /Users/weimengni/my-project/qt-apps/Launcher/build
cmake .. && make backend-test -j4
```

### 2. 运行测试

```bash
./backend-test
```

### 3. 预期输出

```
========================================
Launcher 后端核心流程测试
========================================

=== 测试数据库 ===
✅ 数据库初始化成功
   路径: ".../launcher.db"
   状态: 已打开

=== 测试配置 ===
✅ 配置读写正常
   主题: "auto"
   历史限制: 50

=== 测试应用仓库 ===
✅ 应用加载成功 (UOS 环境)
   数量: 300-500
❌ 应用加载失败 (macOS 环境 - 正常)

=== 测试文件索引器 ===
✅ 文件索引完成
   数量: > 0

=== 测试文件仓库 ===
✅ 最近文件显示正常
✅ 文件搜索正常
========================================
所有测试完成
========================================
```

---

## 详细验证清单

### 验证项 1: 数据库操作

| 测试项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 数据库初始化 | 运行 backend-test | 显示 "✅ 数据库初始化成功" |
| 表创建 | 检查日志 | 显示 4 个表创建成功 |
| 数据库路径 | 检查输出 | 路径为用户目录下 |

**手动验证**:
```bash
# 查看数据库文件
ls ~/Library/Application\ Support/backend-test/

# 使用 sqlite3 检查表结构
sqlite3 ~/Library/Application\ Support/backend-test/launcher.db ".tables"
```

---

### 验证项 2: 配置读写

| 测试项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 写入配置 | 运行 backend-test | 显示 "✅ 配置读写正常" |
| 读取配置 | 检查输出 | 显示默认配置值 |
| 缓存机制 | 代码检查 | Settings 类有 m_cache 成员 |

---

### 验证项 3: 应用加载 (UOS 环境)

| 测试项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 加载系统应用 | 运行 backend-test | 加载 300-500 个应用 |
| 解析 .desktop 文件 | 检查输出 | 显示应用名称和注释 |
| 本地化支持 | 检查中文应用名 | 正确显示中文名称 |

**在 UOS 环境下手动验证**:
```bash
# 检查系统应用目录
ls /usr/share/applications/ | wc -l

# 运行测试
./backend-test

# 验证应用数量是否接近系统应用数量
```

---

### 验证项 4: 应用搜索

| 测试项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 名称匹配 | 搜索 "浏览器" | 返回浏览器类应用 |
| 模糊匹配 | 搜索部分名称 | 返回匹配应用 |
| 空搜索 | 搜索空字符串 | 返回所有应用 |

**手动验证** (UOS 环境):
```bash
# 在测试程序中观察搜索结果
# 测试程序已内置搜索 "浏览器" 的测试
```

---

### 验证项 5: 文件扫描

| 测试项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 扫描用户目录 | 运行 backend-test | 扫描 Documents/Downloads |
| 递归扫描 | 检查文件数量 | 包含子目录文件 |
| 跳过隐藏文件 | 代码检查 | 跳过 .开头的文件 |
| 文件分类 | 检查输出 | 文件有正确的分类 |

**手动验证**:
```bash
# 检查扫描的目录
ls ~/Documents | wc -l
ls ~/Downloads | wc -l

# 对比测试程序的文件数量
./backend-test
```

---

### 验证项 6: 文件搜索

| 测试项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 扩展名搜索 | 搜索 ".pdf" | 返回 PDF 文件 |
| 文件名搜索 | 搜索文件名部分 | 返回匹配文件 |
| 最近文件 | 检查最近文件列表 | 按时间排序 |

---

### 验证项 7: 应用启动 (UOS 环境)

| 测试项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 启动应用 | 调用 launchApplication | 应用成功启动 |
| 清理执行命令 | 代码检查 | 移除 %f %u 等参数 |

**手动验证** (UOS 环境):
```bash
# 需要添加启动测试代码
# 或在集成测试中验证
```

---

### 验证项 8: 文件打开

| 测试项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 打开文件 | 调用 openFile | 系统默认程序打开 |
| 在文件夹显示 | 调用 revealInFileManager | 文件管理器定位文件 |

**手动验证**:
```bash
# 在测试程序中添加打开文件测试
# 或在后续集成测试中验证
```

---

## macOS 环境限制

在 macOS 环境下，以下功能无法完整验证：

| 功能 | 原因 | 解决方案 |
|------|------|----------|
| 应用加载 | 无 /usr/share/applications | 在 UOS 环境验证 |
| 应用启动 | .desktop 文件格式不同 | 在 UOS 环境验证 |
| 文件管理器 | dde-file-manager 不存在 | 在 UOS 环境验证 |

---

## UOS 环境验证

在 UOS 环境下执行完整验证：

```bash
# 1. 构建
cd /path/to/Launcher/build
cmake .. && make backend-test -j4

# 2. 运行测试
./backend-test

# 3. 检查应用数量 (应 > 300)
# 输出: "数量: 300-500"

# 4. 检查搜索功能
# 输出: "找到 X 个结果"

# 5. 检查文件扫描
# 输出: "数量: > 0"
```

---

## 验证通过标准

| 项目 | 标准 | 当前状态 |
|------|------|----------|
| 数据库初始化 | 成功 | ✅ |
| 配置读写 | 正常 | ✅ |
| 应用加载 | > 300 (UOS) | ⏳ 待验证 |
| 文件扫描 | > 0 | ✅ |
| 文件搜索 | 正常 | ✅ |
| 无严重错误 | 无崩溃 | ✅ |

---

## 验证报告

### macOS 环境验证结果

```
日期: 2026-04-07
环境: macOS Darwin 25.3.0

✅ 数据库数据库初始化成功
✅ 配置读写正常
❌ 应用加载失败 (预期行为，macOS 无 .desktop)
✅ 文件索引完成 (129 个文件)
✅ 文件搜索正常
✅ 最近文件显示正常
```

### UOS 环境验证结果

```
日期: 待定
环境: UOS V20 专业版 1070

待在 UOS 环境下验证...
```

---

## 下一步

- [ ] 在 UOS 环境下运行完整验证
- [ ] 确认应用加载数量 > 300
- [ ] 确认应用启动功能
- [ ] 进入阶段 3 开发
